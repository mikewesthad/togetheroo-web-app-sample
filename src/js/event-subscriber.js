/**
 * Event Subscriber
 *
 * A class used in inheritence to give objects an easy way to listen to firebase
 * or pub/sub events in a managed way. It allows for easy unsubscribing to all
 * events when an object needs to clean up after itself.
 */

// OMITTED