/**
 * Toolbox UI
 *
 * This class manages the drawing app's toolbox, which is shared across users in
 * the playspace. This includes responding to user input (changing the selected
 * tool/color, etc.) and publishing/responding to changes in the firebase model.
 */

// OMITTED